# TireWatch Backend - Requirements
# =================================
# Entorno: Windows Server · SQLite · Waitress (WSGI) · sin Docker

# Django + API REST
Django>=4.2,<5.1
djangorestframework>=3.14
django-cors-headers>=4.3
django-filter>=23.5

# Panel de administración moderno (tema del /admin/)
django-jazzmin>=3.0

# Procesamiento de Excel y análisis numérico
pandas>=2.1
openpyxl>=3.1
numpy>=1.26

# Utilidades
python-dotenv>=1.0
requests>=2.31

# Servidor WSGI para Windows + estáticos sin Nginx
waitress>=3.0
whitenoise>=6.7
