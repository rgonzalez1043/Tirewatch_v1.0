# TireWatch Backend - Requirements
# =================================

# Django
Django>=4.2,<5.1
djangorestframework>=3.14
django-cors-headers>=4.3
django-filter>=23.5

# Database (PostgreSQL para producción)
psycopg2-binary>=2.9

# Excel processing
pandas>=2.1
openpyxl>=3.1

# Numeric / analysis
numpy>=1.26

# Utils
python-dotenv>=1.0
gunicorn>=21.2       # Linux/Mac (no usar en Windows)
waitress>=3.0        # Servidor WSGI para Windows
requests>=2.31

# Archivos estáticos en producción (sin Nginx)
whitenoise>=6.7
